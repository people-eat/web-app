import type { Meta, StoryObj } from '@storybook/react';
import HomePageRatingSection from './HomePageRatingSection';

const meta: Meta<typeof HomePageRatingSection> = {
    title: 'Home Page/Rating Section',
    component: HomePageRatingSection,
};

export default meta;

export const Component: StoryObj<typeof HomePageRatingSection> = {};
