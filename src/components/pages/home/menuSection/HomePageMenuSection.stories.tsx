import type { Meta, StoryObj } from '@storybook/react';
import HomePageMenuSection from './HomePageMenuSection';

const meta: Meta<typeof HomePageMenuSection> = {
    title: 'Home Page/Menu Section',
    component: HomePageMenuSection,
};

export default meta;

export const Component: StoryObj<typeof HomePageMenuSection> = {};
