import type { Meta, StoryObj } from '@storybook/react';
import HomePageSection2 from './HomePageSection2';

const meta: Meta<typeof HomePageSection2> = {
    title: 'Home Page/Section 2',
    component: HomePageSection2,
};

export default meta;

export const Component: StoryObj<typeof HomePageSection2> = {};
