query GetProfileQuery {
    users {
        me {
            userId
            firstName
            lastName
            profilePictureUrl
            acceptedTerms
            acceptedPrivacyPolicy
            createdAt
        }
    }
}
