mutation UpdateCookMaximumParticipants($cookId: String!, $maximumParticipants: UnsignedInt) {
    cooks {
        success: updateMaximumParticipants(cookId: $cookId, maximumParticipants: $maximumParticipants)
    }
}
